[project]
name = "navigator-pdf-automation"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
	"fastapi==0.117.1",
	"python-multipart==0.0.20",
	"PyMuPDF==1.24.9",
]

[tool.vercel]
entrypoint = "api.process:app"
